package nuclearnotes;

import java.util.ArrayList;
import java.util.List;

public class GuitarNeck {
    
    private List<GuitarString> guitarStrings = new ArrayList<>();
    
    GuitarString eString = new GuitarString(Notes.E);
    GuitarString BString = new GuitarString(Notes.B);
    GuitarString GString = new GuitarString(Notes.G);
    GuitarString DString = new GuitarString(Notes.D);
    GuitarString AString = new GuitarString(Notes.A);
    GuitarString EString = new GuitarString(Notes.E);
    
    private void addGuitarStrings() {
        
    }
    
    private void draw() {
    
    }
}
